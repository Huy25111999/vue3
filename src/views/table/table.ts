


import { computed, defineComponent, onMounted, reactive, ref, watch } from "vue";

export default defineComponent({
  name: "TablePage",
  components: {
  },
  setup() {
    
   
    return {
    };
  },
});
