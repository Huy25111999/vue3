import { defineComponent } from "vue";

export default defineComponent({
  name: "ModalDelete",
  components: {},
  props: ["title", "show", "content", "onHide", "handleDelete"],
  setup(props, ctx) {

    return {};
  },
});
